services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: blog
      POSTGRES_USER: blogger
      POSTGRES_PASSWORD: blogger
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
